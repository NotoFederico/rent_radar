[Unit]
Description=Rent Radar — Mapa interactivo (HTTP)
After=network.target

[Service]
User=YOUR_USER
WorkingDirectory=YOUR_PROJECT_DIR
ExecStart=YOUR_PROJECT_DIR/venv/bin/python -m http.server 8080
Restart=on-failure
RestartSec=5

[Install]
WantedBy=multi-user.target
